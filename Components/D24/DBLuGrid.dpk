package DBLuGrid;

{$R *.res}
{$IFDEF IMPLICITBUILDING This IFDEF should not be used by users}
{$i package.inc}
{$DESCRIPTION 'JC-Soft Unit'}
{$RUNONLY}
{$IMPLICITBUILD OFF}

requires
  rtl,
  vcl,
  dbrtl,
  vcldb;

contains
  Cmp_DBLookUpGrid in '..\Source\DBLookUpGrid\Cmp_DBLookUpGrid.pas';

end.
