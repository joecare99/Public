package CmpDBConfig;

{$R *.res}
{$IFDEF IMPLICITBUILDING This IFDEF should not be used by users}
{$i package.inc}
{$DESCRIPTION 'JC-Soft Unit'}
{$RUNONLY}
{$IMPLICITBUILD OFF}

requires
  rtl,
  UntConfig,
  vcl,
  dbrtl;

contains
  Cmp_DBConfig in '..\Source\DBConfig\Cmp_DBConfig.pas';

end.
