package DBForms;

{$R *.res}
{$IFDEF IMPLICITBUILDING This IFDEF should not be used by users}
{$i package.inc}
{$DESCRIPTION 'JC-Soft Common DB-Forms'}
{$IMPLICITBUILD OFF}

requires
  rtl,
  vcl,
  dbrtl,
  vcldb,
  adortl;

contains
  Frm_BrowseDB in '..\Frm_BrowseDB.pas' {FBrowseDB},
  Unt_BuildFilter in '..\Unt_BuildFilter.pas';

end.
